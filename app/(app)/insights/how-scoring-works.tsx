import { HowScoringWorksScreen } from '@/features/insights';

/** E4 — `/(app)/insights/how-scoring-works`. No period: the model is the model. */
export default function HowScoringWorksRoute() {
  return <HowScoringWorksScreen />;
}
