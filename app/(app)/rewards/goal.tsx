import { WeeklyGoalScreen } from '@/features/rewards/goal/WeeklyGoalScreen';

/** `/rewards/goal` — F2's weekly goal and its focus picker (the `goal_completed` notification's link). */
export default function WeeklyGoalRoute() {
  return <WeeklyGoalScreen />;
}
