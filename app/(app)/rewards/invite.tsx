import { InviteScreen } from '@/features/referral/InviteScreen';

/** `/rewards/invite` — F10, inviting friends with a code (allowlisted; hidden while the flag is off). */
export default function InviteRoute() {
  return <InviteScreen />;
}
