import { BadgesScreen } from '@/features/rewards/badges/BadgesScreen';

/** `/rewards/badges` — F3, the badge collection. The badge notification opens it. */
export default function BadgesRoute() {
  return <BadgesScreen />;
}
