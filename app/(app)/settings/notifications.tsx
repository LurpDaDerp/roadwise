import { NotificationSettingsScreen } from '@/features/settings/notifications';

/**
 * `/settings/notifications` — H6, notification settings: the categories with a live notification,
 * quiet hours, and what the phone allows. The inbox's gear opens it.
 */
export default function NotificationSettingsRoute() {
  return <NotificationSettingsScreen />;
}
