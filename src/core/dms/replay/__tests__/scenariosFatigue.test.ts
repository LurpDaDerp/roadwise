// The replay matrix, part 3 of 3: the drives long enough for the fatigue score (10 min of learning first).
// DMS_FULL=1 (test-only, see matrix.ts) runs 5 seeds.
import { matrix } from '../__fixtures__/matrix';

matrix(['nodding off', 'drowsy PERCLOS ramp']);
