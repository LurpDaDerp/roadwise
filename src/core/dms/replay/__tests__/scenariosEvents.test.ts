// The replay matrix, part 2 of 3: the distraction and closure events, the lenses, the bump and the driver
// change. DMS_FULL=1 (test-only, see matrix.ts) runs 5 seeds.
import { matrix } from '../__fixtures__/matrix';

matrix(['a 3.0 s infotainment glance', 'a 2.4 s lap glance', 'texting pattern', 'visual time-sharing', 'microsleep', 'sleep', 'falling asleep into LOST (C-26)', 'sunglasses', 'camera bump', 'driver change']);
