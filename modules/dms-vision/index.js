// Entry point for `import DmsVision, { ... } from '../modules/dms-vision'`.
// The implementation lives in src/index.js so the directory keeps the layout that
// `create-expo-module --local` produces (android/, ios/, src/).
export * from './src/index.js';
export { default } from './src/index.js';
