{
  "checkpoint": "checkpoints/gaze_direct_promoted.pt",
  "module1": null,
  "checkpoint_sha256": "b0d3622a51d30a76369b7ab95b281a255f2329db9d1a9747fbabe8b781eacd74",
  "onnx_sha256": "4aa9661091efbdc28b20927f905c78097b9a7b5b787401f74ce6259f8f7666c8",
  "parameters": 867069,
  "model_config": {
    "token_subset": "all478",
    "d_model": 64,
    "depth": 3,
    "heads": 4,
    "dropout": 0.05,
    "geometric_dim": 64,
    "local_channels": true,
    "eye_z_masked": true,
    "use_camera_context": true,
    "pose_head": true,
    "mlp_ratio": 4,
    "arch": "twostream",
    "head_frame_gaze": false,
    "graph_layers": 3,
    "graph_k": 8,
    "eye_d_model": 112,
    "eye_depth": 4,
    "eye_heads": 8,
    "z_masked": false,
    "canonical_channels": false,
    "eye_confidence_channels": false,
    "vertical_invariant_channels": false,
    "subject_conditioning": false,
    "conditioning_dropout": 0.3,
    "eye_gaze_head": false,
    "subject_stats": true,
    "subject_stats_mask": [
      0,
      1,
      0,
      0
    ],
    "subject_stats_mode": "linear",
    "eye_gate": [
      0.45,
      0.65
    ],
    "fusion_dropout": 0.0,
    "output_mode": "vector",
    "confidence_head": false,
    "eye_gate_jitter": 0.0,
    "ray_frame_output": false
  },
  "coordinate_convention": "weak3d: aspect-corrected MediaPipe landmarks centred on the outer-eye midpoint (33, 263), divided by their 2D separation; stored gaze s = diag(1,1,-1) x physical OpenCV direction; horizontal atan2(x, z) +right, vertical atan2(-y, hypot(x, z)) +up",
  "output_frame": "camera",
  "mirror_permutation_sha256": "a93ff2dc0c45ccf523bc24e3da2f0cba8d42f686704cf2bb14184c9146fa92ff",
  "inputs": {
    "cloud": "(B,478,3) weak3d: aspect-corrected, eye-midpoint centred, unit 2D interocular",
    "context": "(B,7): ray_x, ray_y, iod/focal + the four subject statistics (see subject_stats)",
    "validity": "(B,478) float32: 1 = landmark present, 0 = out of frame / occluded (the caller's knowledge; the far-eye visibility gate runs inside the graph)"
  },
  "eye_gate": {
    "low": 0.45,
    "high": 0.65,
    "how": "inside the graph: each eye's lid and iris tokens are scaled by clip((its corner width / the other eye's - low) / (high - low), 0, 1); (0, 0) = off"
  },
  "subject_stats": {
    "names": [
      "aperture",
      "iris_y_in_aperture",
      "upper_lid_v",
      "brow_v"
    ],
    "mask": [
      0,
      1,
      0,
      0
    ],
    "mode": "linear",
    "training_mean": [
      0.3145948052406311,
      -0.022462697699666023,
      -0.21199138462543488,
      -0.9008664488792419
    ],
    "training_scale": [
      0.032676149159669876,
      0.00813260767608881,
      0.02498600259423256,
      0.1125095784664154
    ],
    "how": "per frame: eye-local (aperture / width, iris centre minus lid-aperture centre along the eye vertical, upper-lid level, brow level), mean of both eyes (left eye's signed axes flipped); the context carries the driver's expanding causal MEDIAN over the frames seen so far (raw units; the graph centres / scales them).  For the first 30 frames pass training_mean (a zero offset)."
  },
  "mirror_tta": "run on [cloud; mirror(cloud)] with context x negated; average after negating the mirrored gaze x",
  "parity_max_abs_gaze": 1.341104507446289e-07
}
