Urgent!!!
1) fix google sign in 
2) optimize location updates to stop cooking backend
3) optimize firestore data organization for less reads and better security

Important
1) put in driving distraction model (in progress)
2) update driving points logic to account for other metrics

Chill
1) check metrics accuracy for driving 
2) update tracked metrics and organize them better in firestore
3) group metrics display in app by category instead of just list
3) location screen lag
4) UI polish
5) redundant logic checks
6) better settings features (and sync w/ firestore instead of just local asyncstorage)

Optional
1) ensure android compatibility
2) event timeline for each drive that user can view

Future consideration
1) rewards integration w/ rewards partner companies
2) monetization
    - paid features (expensive apis)
    - ads?? (NOT on driving interface)